<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aescobar68.applecturas">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.intent.action.MEDIA_MOUNTED" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logop_solo"
        android:label="Pegasolabel"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".lecturasActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".menuprincipal" />
        <!--
 ATTENTION: This was auto-generated to add Google Play services to your project for
     App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information.
        -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity android:name=".AboutActivity" />
        <activity android:name=".TotalizarActivity" />
        <activity android:name=".ActualizarActivity" />
        <activity android:name=".BuscarActivity" />
        <activity
            android:name=".Tomar_fotoActivity"
            android:configChanges="screenSize|orientation" />
        <!-- Evitamos que el imagenview de la foto se borre al girar la pantalla -->
        <activity android:name=".AnomaliaActivity" />
        <activity android:name=".Enviar_fotografiasActivity" />
        <activity android:name=".NoEnviadasActivity" />
        <activity android:name=".TotalRutaActivity" />
        <activity android:name=".Preferencias" />
        <activity android:name=".abrir_rutaActivity"></activity>
    </application>

</manifest>